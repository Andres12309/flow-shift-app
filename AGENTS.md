# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# FlowShift

Ver `ARCHITECTURE.md` y `.cursor/rules/` para arquitectura, SQLite y notificaciones parametrizables.
